/* tslint:disable:no-unused-variable */

import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject
} from '@angular/core/testing';
import {Gofa} from './gofa';

describe('Gofa', () => {
  it('should create an instance', () => {
    expect(new Gofa()).toBeTruthy();
  });
});
